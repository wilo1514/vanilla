# n8n Local

Local Docker n8n setup for The Vanilla Republic automation system.

Use `docker compose up` from this folder after confirming the required `.env` values are present locally. Do not commit `.env` files or n8n runtime data.
